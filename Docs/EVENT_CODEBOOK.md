# Choice Overload — Event Codebook

## Version

Experiment version:

```text
v0.3.0
```

Event-codebook version:

```text
1.0
```

This document describes the event markers generated by the Choice Overload experiment for EEG and eye-tracking synchronization.

The MATLAB source of truth is:

```matlab
P.Events
```

defined in:

```text
Initialize/initializeParameters.m
```

The exact codebook used during each participant session is also stored in:

```matlab
R.Events
```

inside the participant result file.

---

# Event Routing

All task-level events are routed through:

```matlab
sendEvent(P, T, taskName, eventName, eventCode)
```

Two forms of event information are used:

- `eventCode` — numeric trigger intended for EEG
- `eventName` — descriptive event message intended for systems such as eye tracking

Task routing currently supports:

```text
PreferenceRating
Choice
```

Questionnaire events are routed through:

```text
Choice
```

because the questionnaire occurs as part of successful Choice trials.

---

# Experiment Events

| Code | Event |
|---:|---|
| 1 | Experiment Start |
| 2 | Experiment End |

These codes are reserved in `v0.3.0`.

Experiment-level routing will be finalized together with the actual acquisition hardware lifecycle.

They are not currently emitted by the behavioral task.

---

# Preference Rating

## Practice

| Code | Event |
|---:|---|
| 10 | Practice Stimulus |
| 11 | Practice Response |

Typical event names:

```text
RATING_PRACTICE_STIMULUS
RATING_PRACTICE_RESPONSE rating=<value>
```

## Main Rating Rounds

| Code | Event |
|---:|---|
| 12 | Rating Round Start |
| 13 | Rating Round End |
| 14 | Rating Stimulus |
| 15 | Rating Response |

Typical event names:

```text
RATING_ROUND_START round=<round>
RATING_STIMULUS
RATING_RESPONSE rating=<value>
RATING_ROUND_END round=<round>
```

Example:

```text
RATING_ROUND_START round=1
RATING_STIMULUS
RATING_RESPONSE rating=6.25
RATING_STIMULUS
RATING_RESPONSE rating=4.5
RATING_ROUND_END round=1
```

---

# Choice Task

## General Choice Events

| Code | Event |
|---:|---|
| 20 | Choice Block Start |
| 21 | Choice Block End |
| 22 | Fixation Onset |
| 23 | Mask Onset |
| 24 | Choice Response Onset |
| 25 | Valid Choice Response |
| 26 | Choice Miss |

Typical event names:

```text
CHOICE_BLOCK_START block=<block> setSize=<size>
CHOICE_BLOCK_END block=<block> setSize=<size>

CHOICE_FIXATION setSize=<size> condition=<condition>
CHOICE_MASK setSize=<size> condition=<condition>

CHOICE_RESPONSE_ONSET setSize=<size> condition=<condition>

CHOICE_RESPONSE setSize=<size> condition=<condition> location=<location> RT=<seconds>

CHOICE_MISS setSize=<size> condition=<condition> elapsed=<seconds>
```

---

# Choice Exposure Events

Choice exposure is the primary experimental manipulation.

Exposure codes uniquely identify:

```text
Set Size × Experimental Condition
```

Current set sizes:

```text
6
12
24
```

Current experimental conditions:

| Abbreviation | Condition |
|---|---|
| UL | Uniform Least |
| UM | Uniform Moderate |
| UH | Uniform High |
| CF | Clearly Favored |
| RS | Random Set |

## Exposure Code Matrix

| Set Size | UL | UM | UH | CF | RS |
|---:|---:|---:|---:|---:|---:|
| 6 | 30 | 31 | 32 | 33 | 34 |
| 12 | 35 | 36 | 37 | 38 | 39 |
| 24 | 40 | 41 | 42 | 43 | 44 |

Examples:

```text
30 = 6-item Uniform Least exposure
33 = 6-item Clearly Favored exposure
38 = 12-item Clearly Favored exposure
44 = 24-item Random Set exposure
```

Typical eye-tracker event name:

```text
CHOICE_EXPOSURE setSize=12 condition=CF
```

Corresponding EEG trigger:

```text
38
```

Exposure codes are resolved dynamically by:

```matlab
getChoiceExposureCode(P, setSize, condition)
```

---

# Questionnaire

Questionnaire marker codes are generated dynamically from:

```matlab
P.Questionnaire.nQuestions
```

For experiment version `v0.3.0`:

```matlab
P.Questionnaire.nQuestions = 3;
```

Therefore the current mapping is:

| Question | Onset | Response | Timeout |
|---:|---:|---:|---:|
| Q1 | 100 | 103 | 106 |
| Q2 | 101 | 104 | 107 |
| Q3 | 102 | 105 | 108 |

## Questionnaire Onset

| Code | Event |
|---:|---|
| 100 | Question 1 Onset |
| 101 | Question 2 Onset |
| 102 | Question 3 Onset |

Typical event names:

```text
QUESTIONNAIRE_ONSET question=1
QUESTIONNAIRE_ONSET question=2
QUESTIONNAIRE_ONSET question=3
```

## Questionnaire Response

| Code | Event |
|---:|---|
| 103 | Question 1 Response |
| 104 | Question 2 Response |
| 105 | Question 3 Response |

Typical event name:

```text
QUESTIONNAIRE_RESPONSE question=<question> value=<value> RT=<seconds>
```

Example:

```text
QUESTIONNAIRE_RESPONSE question=2 value=7 RT=1.428392
```

## Questionnaire Timeout

| Code | Event |
|---:|---|
| 106 | Question 1 Timeout |
| 107 | Question 2 Timeout |
| 108 | Question 3 Timeout |

Typical event name:

```text
QUESTIONNAIRE_TIMEOUT question=<question> elapsed=<seconds>
```

Example:

```text
QUESTIONNAIRE_TIMEOUT question=3 elapsed=6.000821
```

---

# Typical Successful Choice Trial

A successful Choice trial produces approximately:

```text
22       CHOICE_FIXATION
30–44    CHOICE_EXPOSURE
23       CHOICE_MASK
24       CHOICE_RESPONSE_ONSET
25       CHOICE_RESPONSE
100      QUESTIONNAIRE_ONSET question=1
103      QUESTIONNAIRE_RESPONSE question=1
101      QUESTIONNAIRE_ONSET question=2
104      QUESTIONNAIRE_RESPONSE question=2
102      QUESTIONNAIRE_ONSET question=3
105      QUESTIONNAIRE_RESPONSE question=3
```

The exact exposure code depends on the current set size and condition.

---

# Typical Missed Choice Trial

A missed Choice trial produces:

```text
22       CHOICE_FIXATION
30–44    CHOICE_EXPOSURE
23       CHOICE_MASK
24       CHOICE_RESPONSE_ONSET
26       CHOICE_MISS
```

The questionnaire is skipped after a missed Choice response.

---

# Preference Rating Sequence

Practice trial:

```text
10       RATING_PRACTICE_STIMULUS
11       RATING_PRACTICE_RESPONSE
```

Main experimental round:

```text
12       RATING_ROUND_START
14       RATING_STIMULUS
15       RATING_RESPONSE
14       RATING_STIMULUS
15       RATING_RESPONSE
...
13       RATING_ROUND_END
```

---

# Timing Semantics

Stimulus-onset markers are sent immediately after their corresponding Psychtoolbox:

```matlab
Screen('Flip', ...)
```

Response markers are based on the mouse state sampled during response collection.

Reaction times are measured relative to the corresponding onset flip.

Choice fixation, exposure, and mask durations use flip-anchored absolute timing:

```matlab
WaitSecs('UntilTime', onset + duration);
```

This prevents event-transmission execution time from extending the intended visual duration.

---

# Trigger Range

The current codebook is designed for an 8-bit EEG trigger interface.

Valid numeric range:

```text
0–255
```

At experiment initialization, MATLAB verifies:

- no configured event code exceeds `255`
- no two configured event codes are identical

If either condition fails, experiment initialization stops with an error.

---

# Codebook Summary

```text
01    Experiment Start                  [reserved]
02    Experiment End                    [reserved]

10    Rating Practice Stimulus
11    Rating Practice Response
12    Rating Round Start
13    Rating Round End
14    Rating Stimulus
15    Rating Response

20    Choice Block Start
21    Choice Block End
22    Choice Fixation
23    Choice Mask
24    Choice Response Onset
25    Choice Response
26    Choice Miss

30    Exposure — 6  / UL
31    Exposure — 6  / UM
32    Exposure — 6  / UH
33    Exposure — 6  / CF
34    Exposure — 6  / RS

35    Exposure — 12 / UL
36    Exposure — 12 / UM
37    Exposure — 12 / UH
38    Exposure — 12 / CF
39    Exposure — 12 / RS

40    Exposure — 24 / UL
41    Exposure — 24 / UM
42    Exposure — 24 / UH
43    Exposure — 24 / CF
44    Exposure — 24 / RS

100   Questionnaire Q1 Onset
101   Questionnaire Q2 Onset
102   Questionnaire Q3 Onset

103   Questionnaire Q1 Response
104   Questionnaire Q2 Response
105   Questionnaire Q3 Response

106   Questionnaire Q1 Timeout
107   Questionnaire Q2 Timeout
108   Questionnaire Q3 Timeout
```

---

## Codebook Stability

This document describes:

```text
P.Events.version = 1.0
```

Any future change to the meaning or numeric value of an event marker should increment the event-codebook version and update this document accordingly.
